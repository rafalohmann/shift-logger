import { createMuiTheme } from '@material-ui/core/styles'

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#37b24a',
      contrastText: '#fff'
    },
    secondary: {
      main: '#3a3a3a',
      contrastText: '#fff'
    }
  }
})

export default theme
